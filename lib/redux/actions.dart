var _login_token = null;

class SucceedLoginActions {
  var loginToken = _login_token;
  SucceedLoginActions(this.loginToken);
}

class IfLoginAction {
  var loginToken = _login_token;
  IfLoginAction(this.loginToken);
}